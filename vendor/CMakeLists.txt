# SPDX-License-Identifier: Apache-2.0
# Build llm_inference_interactive against TensorRT Edge-LLM SDK
#
# Usage: add to Edge-LLM build via build_in_container.sh, or:
#   cd TensorRT-Edge-LLM/build
#   cmake .. [flags] && make llm_inference_interactive

# This file is NOT used directly — it documents the build target.
# The actual target is added to Edge-LLM's examples/llm/CMakeLists.txt
# by the build script.
