# Example environment variables for Semantic server
# Copy to .env and fill in values (never commit .env to git!)

# API Key (if implementing auth in future)
# SEMANTIC_API_KEY=your-secret-key-here

# Override model
# SEMANTIC_MLX__MODEL_ID=mlx-community/Llama-3.1-8B-Instruct-4bit

# Override server settings
# SEMANTIC_SERVER__HOST=0.0.0.0
# SEMANTIC_SERVER__PORT=8080
# SEMANTIC_SERVER__ALLOW_REMOTE=true

# Override cache directory
# SEMANTIC_AGENT__CACHE_DIR=/custom/cache/path

# Logging level
# SEMANTIC_LOGGING__LEVEL=DEBUG

# Metrics
# SEMANTIC_METRICS__ENABLED=true
# SEMANTIC_METRICS__PORT=9090

# Note: Environment variable format is SEMANTIC_<SECTION>__<KEY>
# Example: [mlx] model_id → SEMANTIC_MLX__MODEL_ID
